import { Configuration } from 'webpack';
/**
 * Requires in the user's Webpack config
 */
declare const requireWebpackConfig: (webpackConfig: string, required?: boolean, env?: string, mode?: "none" | "development" | "production") => Promise<Configuration>;
export default requireWebpackConfig;
